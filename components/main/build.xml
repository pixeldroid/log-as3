<?xml version="1.0" encoding="UTF-8"?>

<project name="as3-log.main" default="-usage" basedir=".">

<description>
Builds and releases the core as3-log swc.
</description>
	
	<property name="dir.root" location="${basedir}/../.."/>
	<import file="${dir.root}/tools/ant/init.xml"/>
	
	
	<target name="all" description="&gt; this is the main task (calls component sub-builds)">
		<ant antfile="${basedir}/build/main.build.xml" target="all"/>
	</target>
	
</project>

